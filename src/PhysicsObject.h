#pragma once
#include <SFML/Graphics.hpp>
#include "Gravity.h"
#include "vectors.h"
#include "matrices.h"
/*
class PhysicsObject
{
	

public:
	vec2 origin = { 0,0 };
	vec2 size = { 0,0 };
	Gravity gravity;
	vec2 velocity = { 0, 0 };
	mat3 transform;

	bool gravityOn;

	PhysicsObject(vec2 origin , bool gravityOn );

	virtual void update(float deltaTime); 

	virtual void draw(sf::RenderWindow& window ) = 0;

	virtual ~PhysicsObject() = default;
};
*/
